# 切换生产环境地址使用
prod:
  host: 192.168.8.116
server:
  port: 8771

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

spring:
  application:
    name: config
  profiles:
    active: dev
  rabbitmq:
    host: ${prod.host}
  cloud:
    config:
      server:
        git:
          uri: https://github.com/SpringCloud-Sell/config-repo.git
          username: Rushing0711
          password: Rushing@0711
          basedir: /home/emon/saas/SpringCloud-Sell/config/basedir
management:
  endpoints:
    web:
      exposure:
        include: "*"
